// types — shared UI-only types for the details panel sub-components
import type { ResourceType } from "../../models/resource";

export type EditableResource = { title: string; type: ResourceType; link: string };
